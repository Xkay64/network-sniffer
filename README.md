# Network Sniffer

A Python program that captures and analyzes network traffic to help understand data flow and packet structure on a network.

## Features
- Captures packets
- Parses Ethernet, IP, TCP/UDP headers
- Displays summary of captured packets

## Usage
Run the script with root privileges:
```bash
sudo python3 sniffer.py
